#include "dimaxl.h"

Item::Item(const float start_point, const float end_point) : 
  start_point_(start_point), end_point_(end_point)
{
}

DiMaxL::Solve()
{
  int s = GetIntervals();
  int q = GetPoints();
  for(int t = 0; t < s; t++) {
    for(int q = 0; q <= p; q++) {
    }
  }
}

